# Python
__pycache__/
*.pyc
*.pyo
*.pyd
venv/
.venv/
*.egg-info/
.eggs/

# Test / lint artifacts
.pytest_cache/
.coverage
htmlcov/
.ruff_cache/

# Output files generated by the tool
photo_check_results.csv
photo_check_results.json
*.csv
# Note: *.json would exclude package.json — scope it to the results folder instead
results/*.json
results/*-meta.json

# Credentials and cache live in ~/.photo_checker/ — never committed

# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Build artifacts
build/
dist/

# Node / Next.js
node_modules/
.next/
web/out/
web/tsconfig.tsbuildinfo
.env.local
.env*.local

# Secrets and tokens (belt-and-suspenders)
*.token
*.pem
*_token.json
*_cache.bin
