# Optional dependencies for Google Photos and the legacy Streamlit viewer.
# Install only if you need these features:
#   pip install -r requirements-optional.txt
#
# OneDrive has NO Python dependency — it uses the `rclone` CLI (brew install rclone).
google-auth-oauthlib
google-auth-httplib2
streamlit
pandas
